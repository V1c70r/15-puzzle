describe('EnI18n', () => {
  describe('constructor', () => {
    it('sets a board config');
  });

  describe('start', () => {
    it('has not empty test after start');
  });

  describe('stop', () => {
    it('must not fail');
  });

  describe('cantMoveNumber', () => {
    it('returns a string that contains given number');
  });
});
